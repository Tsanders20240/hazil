const menuBtn=document.querySelector('.menuBtn');
const navlinks=document.querySelector('.navlinks');
if(menuBtn&&navlinks){menuBtn.addEventListener('click',()=>{const open=navlinks.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});navlinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navlinks.classList.remove('open');menuBtn.setAttribute('aria-expanded','false')}));}
const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduce&&'IntersectionObserver'in window){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));}else{document.querySelectorAll('.reveal').forEach(el=>el.classList.add('in'));}
const modal=document.querySelector('#videoModal');const modalBox=modal?.querySelector('.modalVideo');let lastFocus=null;
function event(name,params={}){try{window.dataLayer=window.dataLayer||[];window.dataLayer.push({event:name,...params});}catch{}}
function openVideo(id){if(!modal||!modalBox)return;lastFocus=document.activeElement;modalBox.innerHTML=`<iframe src="https://www.youtube-nocookie.com/embed/${id}?autoplay=1&rel=0&modestbranding=1" title="HÄZIL KRUCIAL official video" allow="autoplay; encrypted-media; picture-in-picture; web-share" allowfullscreen></iframe>`;modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';modal.querySelector('.modalClose')?.focus();event('krucial_video_open',{video_id:id});}
function closeVideo(){if(!modal)return;modal.classList.remove('open');modal.setAttribute('aria-hidden','true');if(modalBox)modalBox.innerHTML='';document.body.style.overflow='';lastFocus?.focus?.();}
document.querySelectorAll('[data-video]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();openVideo(el.dataset.video)}));modal?.querySelector('.modalClose')?.addEventListener('click',closeVideo);modal?.addEventListener('click',e=>{if(e.target===modal)closeVideo()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal?.classList.contains('open'))closeVideo()});
async function postJson(endpoint,payload,statusEl,successEvent){statusEl.textContent='Sending…';try{const res=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const body=await res.json().catch(()=>({}));if(!res.ok)throw new Error(body.error||'Unable to submit right now.');statusEl.textContent=body.message||'Received. Thank you.';event(successEvent);return true;}catch(err){statusEl.textContent=err.message||'Unable to submit right now. Please try again later.';return false;}}
const fanForm=document.querySelector('#fanSignup');
if(fanForm){fanForm.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(fanForm);const ok=await postJson('/api/fan-signup',{name:String(fd.get('name')||'').trim(),city:String(fd.get('city')||'').trim(),email:String(fd.get('email')||'').trim(),website:String(fd.get('website')||''),consent:fd.get('consent')==='yes',source:'hazil.atechspot.com/krucial'},document.querySelector('#fanStatus'),'krucial_fan_signup');if(ok)fanForm.reset();});}
const inquiryForm=document.querySelector('#inquiryForm');
if(inquiryForm){inquiryForm.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(inquiryForm);const ok=await postJson('/api/business-inquiry',{name:String(fd.get('name')||'').trim(),email:String(fd.get('email')||'').trim(),phone:String(fd.get('phone')||'').trim(),org:String(fd.get('organization')||'').trim(),date:String(fd.get('date')||'').trim(),city:String(fd.get('location')||'').trim(),type:String(fd.get('type')||'').trim(),budget:String(fd.get('budget')||'').trim(),message:String(fd.get('message')||'').trim(),website:String(fd.get('website')||''),source:'hazil.atechspot.com/krucial'},document.querySelector('#inquiryStatus'),'krucial_business_inquiry');if(ok)inquiryForm.reset();});}